release:
  stage: release
  image: goreleaser/goreleaser:v2.12.7
  rules:
    - if: '$CI_COMMIT_TAG =~ /^v/'
  variables:
    GIT_DEPTH: "0"
  script:
{{- $cmds := index .FileScripts "release.yml" }}
{{- range $cmd := $cmds }}
    - {{ oneLine $cmd }}
{{- end }}
