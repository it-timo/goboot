/*
Package config defines the configuration structures used by {{.ProjectName}}.

It includes typed representations of all project-level settings used during generation, templating, or tool behavior.

These configurations are typically loaded from static files and validated before use.
*/
package config
