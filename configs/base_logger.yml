# yaml-language-server: $schema=../schemas/base_logger.schema.json
# Config for the `base_logger` service.
# Safe to commit; no secrets.

# Logger backend to scaffold.
# Supported values: "slog", "zerolog".
loggerType: "slog"
