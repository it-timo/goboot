# Config for the `base_local` service.
# Safe to commit; no secrets.

# Template source.

#  Directory where the templates will be read from
sourcePath: "templates/local_base"

# Local outputs to generate.
# Files render only when commands/content are registered for them.
fileList:
  - make
  - task
  - script
  - commit
