# yaml-language-server: $schema=../schemas/base_test.schema.json
# Config for the `base_test` service.
# Safe to commit; no secrets.

# Template source.

#  Directory where the templates will be read from.
sourcePath: "templates/test_base"

# Test behavior.

#  Optional explicit style. Empty uses the root profile default.
#  Supported explicit values are "ginkgo" and "go".
useStyle: ""

#  Optional explicit command. Empty uses the root profile baseline.
testCmd: ""
