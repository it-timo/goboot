# Config for the `base_governance` service.
# Safe to commit; no secrets or private security contacts.

# Template source.
sourcePath: "./templates/governance_base"

# Provider handles written to CODEOWNERS.
maintainers:
  - "@it-timo"

# Branch targeted by contribution templates.
defaultBranch: "main"
